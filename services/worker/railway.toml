[build]
builder = "DOCKERFILE"
dockerfilePath = "services/worker/Dockerfile"
watchPatterns = ["services/api/**", "services/worker/**", "apps/web/package.json", "pnpm-lock.yaml", ".env.example", "README.md"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
